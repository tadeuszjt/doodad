module main

import lang/lexer
import lang/parser
import std/vec2

fn main()
    let t = lexer.TokOp("+")
    print(t)

    let (tokens, ss) = lexer.lexTokens("22378x-benis")
    print(ss)

    let p = parseTerm(tokens)

    for [i] p
        print(p[i])
