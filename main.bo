module main

import lang/lexer
import lang/parser
import std/vec2

fn main()
    let str = "12 + 7-4 * 33"

    let (tokens, rest) = lexer.lexTokens(str)
    print(tokens, rest)

    let (n, ts) = parser.parseExpr(tokens)
    print(n, ts)

