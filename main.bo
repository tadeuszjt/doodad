module main

imports std/strings
imports std/io
imports lang/lexer
imports lang/parser

fn main()
    let str = "benis"

    let loop = true

    while loop
        switch lexToken(str)
            (null, ss); loop = false
            (tok,  ss)
                print(tok)
                str  = ss
