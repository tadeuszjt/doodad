module main

import lang/lexer
import lang/parser
import std/vec2

fn main()
    let (tokens, ss) = lexTokens("22378x-benis")
    print(ss)

    let p = parseTerm(tokens)

    let i = 0
    while i < len(p)
        print(p[i])
        i = i + 1
