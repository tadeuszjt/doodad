module main
import std/vec2
import std/maths
import std/rect
import std/strings

fn main()
        let va = Vec2(1.0, 3.0)
        let vb = Vec2(9, 7)
        vb = (2, 1)
        print("5:", length(va + vb))

        let str = "tadeusztomoszek"
        print("true:", strIsPrefix("tadeusz", str))
        print("false:", strIsPrefix("Tadeusz", str))
        print("false:", strIsPrefix("tadeusztomoszekiscool", str))
