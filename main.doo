module main
import std/vec2
import std/maths
import std/rect
import std/strings
import std/io
import std/rand
import std/assert
import std/chars
import std/regex

import_c stdlib.h
import_c stdio.h

fn main()
    let v = Vec2(1, 2)
    print(v, v.x, v.y, v.length())

    data regexStr String "ab.*B.A"
    data textStr String "doodad ab benis BDA"
    print({regexStr, textStr}.match())




