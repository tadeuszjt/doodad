module main

import std/io
import std/strings


fn {io Io} main()
    let key = io.openFile("file.txt")
    data line String 
    {io, line}.readLn(key)
    {io, line}.writeLn()



    io.closeFile(key)
    

