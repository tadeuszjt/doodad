module io

extern getchar() char;
extern putchar(c char);

fn getStrLn() {char}
	let str = {char}();
	while true
		switch getchar()
			c    : str = append(str, c);
			'\n' : return str;


fn putStrLn(str {char})
	let i = 0;
	while i < len(str)
		putchar(str[i]);
		i = i + 1;

