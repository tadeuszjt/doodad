module io

import_c stdio.h


fn getChar() char
    return char(c::getchar())


fn putChar(c char)
    c::putchar(i32(c))


fn putStr(s string)
    for s -> c; putChar(c)

fn putStrLn(s string)
    for s -> c; putChar(c)
    putChar('\n')
