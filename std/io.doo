module io

import_c stdio.h


fn getChar() char
    return char(c::getchar())


fn putChar(c char)
    c::putchar(i32(c))


fn putStr(s string)
    for [i] s; putChar(s[i])

fn putStrLn(s string)
    for [i] s; putChar(s[i])
    putChar('\n')
