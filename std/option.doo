module option

import builtin/builtin
import builtin/store
import builtin/compare

enum{T} Option {
    none
    some(T)
}

derives{T} T.Option (store)
derives{T} T.Option (equal)
derives{T} T.Option (destroy)
