module for

import std/container


feature{T}    forBegin(T) I64
feature{T}    forEnd(T) I64
feature{T, E} forAt(T{E}, I64) E


aquires{T} forBegin{ T.Slice } (s&);    return 0
aquires{T} forEnd{ T.Slice } (s&);      return s.len
aquires{T} forAt{ Slice, T } (s&, idx); return s[idx]
