module rect

import vec2
import strings

type Rect (min Vec2, max Vec2)

fn {r Rect} width(); return r.max.x - r.min.x
fn {r Rect} height(); return r.max.y - r.min.y
fn {r Rect} area(); return (r.max.x - r.min.x) * (r.max.y - r.min.y)

