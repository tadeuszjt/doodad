module array


fn{T, S} at(array &Array{T, S}, index I64) &T
    return builtin_array_at(&array, index)


fn{T, S} len(array &Array{T, S}) I64
    let len
    ${ $len = sizeof($array->arr) / sizeof($array->arr[0]); }
    return len
