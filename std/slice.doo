module slice

import std/integer
import std/features


fn{T} Len::len(s &[]T) I64
    let n = 0
    ${ $n = $s->len; }
    return n

fn{T} At::at(s &[]T, idx I64) &T
    return builtin_slice_at(&s, idx)
