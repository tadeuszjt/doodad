module bool

fn set(a &Bool, b Bool)
    ${ *$a = $b; }

fn andAnd(a Bool, b Bool) Bool
    let c : Bool
    ${ $c = $a && $b; }
    return c

fn not(a Bool) Bool
    let c : Bool
    ${ $c = $a ? 0 : 1; }
    return c
