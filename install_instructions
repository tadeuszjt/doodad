1.) Build and install correct llvm version for llvm-hs
2.) Install ghc
3.) Install cabal with cabal-install, update, upgrade, update
4.) cabal install llvm-hs, cabal install llvm-hs-pure --lib
5.) Make sure clang is a high enough version
