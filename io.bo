module io

extern getchar() char;
extern putchar(c char);

fn getStrLn() {char} {
    let str = ({char})();
    let loop = true;

    while loop {
        switch getchar() {
            '\n': loop = false;
            c   : str  = append(str, c);
        }
    }

    return str;
}

fn putStrLn(str {char}) {
    let i = 0;

    while i < len(str) {
        putchar(str[i]);
        i = i + 1;
    }

    putchar('\n');
}
