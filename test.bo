module main

imports std/strings
imports std/io
imports lang/lexer

fn main()
    let str = ""
    let loop = true

    while loop
        switch getStrLn()
            s | strEqual("\0", s) ; loop = false
            s ; str = append(append(str, s), ['\n'])


    let toks = :[Token]()
    loop = true

    while loop
        switch lexToken(str)
            (null, ss); loop = false
            (tok,  ss)
                str  = ss
                switch tok
                    TokIdent(s)   ; print("ident  ", s)
                    TokOp(s)      ; print("op     ", s)
                    TokKeyword(s) ; print("keyword", s)
                    TokInt(n)     ; print("integer", n)
                    TokSym(c)     ; print("symbol ", c)

    print(str)
            

