module palindrome
import ../std/strings
import ../std/io

fn main()
    while true
        let s = getStrLn()
        if strings::reverse(s) == s
            print("is palindrome!")
        else
            print("is not palindrome!")
