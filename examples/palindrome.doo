module palindrome
import ../std/strings
import ../std/io

fn main()
    while true
        switch getStrLn()
            "";
            "\0"; return
            s | s == strings::reverse(s)
                print(s + " is a palindrome")
            s; print(s + " is not a palindrome")

