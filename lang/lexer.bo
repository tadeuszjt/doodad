module lexer

imports ../std/io
imports ../std/strings

type TokSym String
type TokInt i64

type Token {TokSym, TokInt}


fn parseLet(str String) (:Token, :String)
    let s = String("let")
    let t = Token(&TokSym(s))

    if isPrefix(s, str)
        return (t, str)

    return (t, str)
